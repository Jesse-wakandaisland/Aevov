import React from 'react';

function GlobalSearchBar() {
    return (
        <div>
            <input type="text" placeholder="Search for patterns, chunks, etc." />
        </div>
    );
}

export default GlobalSearchBar;
